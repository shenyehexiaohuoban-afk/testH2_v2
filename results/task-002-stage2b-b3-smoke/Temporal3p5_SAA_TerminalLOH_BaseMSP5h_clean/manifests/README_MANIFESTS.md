# Clean template manifests

This directory contains package-contract templates only. It intentionally has
no historical run manifest, checkpoint, OOS bank, analysis output, or acceptance
result. A derived experiment must create its own run manifest.

Identity: `BASE_STAGE89Q_REFERENCE`; Pmax `[300,200,120,150]` kW; terminal
mode `DIRECT_GAP`; terminal redistribution `OFF`; terminal gap penalty `1000`;
Stage90 `OFF`.
