# Template provenance

This file records program provenance only.  No historical result, checkpoint,
OOS row, KPI, or analysis artifact is copied into the template.

TRAINING_TEMPLATE_PROVENANCE = derived/verified against the successful B0001 3 h formal training chain (fresh, zero-cut, checkpoint lifecycle, heartbeat, status, and clean-process handoff).

OOS_TEMPLATE_PROVENANCE = derived/verified against the B0001 formal 10000-path OOS chain (common bank, checkpoint identity, path ledger, raw chunks, and output manifest).

ANALYSIS_TEMPLATE_PROVENANCE = derived/verified against the stage89q-b0001-c-mode-readonly-reanalysis/run-016 program chain (Level 1, decomposition, pairing, target groups, time/mechanism/tail/closeout coverage).

Historical identifiers in this provenance statement are source references only;
they are not active paths or runtime defaults.
