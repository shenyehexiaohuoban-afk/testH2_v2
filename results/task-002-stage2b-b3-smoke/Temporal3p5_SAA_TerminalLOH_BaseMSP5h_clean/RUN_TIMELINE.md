# Temporal3p5 DRO BaseMSP5h Run Timeline

Candidate identity: `TEMPORAL3P5_DRO_BASEMSP5H_CANDIDATE`

Status: `NOT YET FORMALLY ADOPTED`

- Duration-aware Pearson-DRO completed: 35/35 optimal.
- Frozen TerminalLOH SHA-256: `70e02fe1b46d09b4cf77fcf1fbfa1f1c4ab98c8200c52b51d6bdd7ac2205dae5`.
- Canonical MSP framework: Stage89Q Base, Pmax `[300,200,120,150]` kW, tank `[300,200,100,200]` kg, 6 x 8 h, direct terminal gap, penalty 1000 yuan/kg, k_init 81.
- Formal training budget: fresh zero cuts, seed 20260513, fixed 18000 seconds.
- Mode C OOS is gated on final checkpoint freeze.
- Interface smoke: PASS; candidate TerminalLOH hash, 35-state mapping, k_init 81, initial solve, and MAT round-trip verified.
- Formal-loop lifecycle smoke: PASS on `smoke-20260901-015738`; one full forward/backward iteration, 1,051 cuts, final checkpoint SHA `2d8b7c73d2be119bedba6a1b8a34a044510c37b372b49918f8f1d033ea3646f7`, and clean reload PASS.
- Auto-repair record: two pre-policy smoke attempts found path/dependency layout issues (`mc_sample.m`); no scientific input, cut, or checkpoint was changed. The verified formal package `program/utils` dependency was copied byte-for-byte and the lifecycle smoke then passed.
- Formal MSP training started: `2026-09-01 02:00:54 +08:00`, run `run-20260901-020054`, fresh zero cuts, fixed 18,000 s budget, Base Pmax `[300,200,120,150]`, OOS disabled.
- Startup health check (`2026-09-01 02:02:34 +08:00`): launcher and MATLAB alive; iteration 2; elapsed 89 s; cuts 2,102; LB `44073.4544062516`; finite outputs; no fatal exception, infeasibility, NaN, or Inf; disk free about 182.8 GB. The two missing-directory warnings refer to inactive legacy paths and did not affect active program resolution.
- While formal training remained active, the downstream chain was prepared without running OOS: `START_OOS.ps1` accepts only a formal completed/frozen checkpoint and exposes only the `BANK` and read-only `OOS` phases; its early gate test correctly rejected execution before training completion.
- The candidate-only Mode C analyzer and `START_ANALYSIS.ps1` were added and passed Python/PowerShell syntax checks. The analysis gate correctly rejected execution because `OOS_COMPLETED.txt` does not yet exist. No candidate OOS row has been generated and no Base/candidate raw input has been modified.
- Mode C scope was mechanically checked against `codex_rule/OOS_rule.md`. Planned outputs cover identity, ordered common paths, termination ledger, target groups, capped mass service, quantity/location/mixed decomposition, migrations, time/site/system/HTT mechanisms, Base-frozen tail groups, costs, optimistic recoverability, historical issues, and evidence boundaries. Full-path substation capacity/headroom is explicitly `NOT_IDENTIFIABLE` because the inherited raw schema stores import but not the frozen capacity/headroom field; it will not be inferred from the single reference-path diagnostic.
- Pre-1h progress observation (`2026-09-01 02:41 +08:00`): iteration 46; elapsed about 2,413 s; cuts 48,346; LB `55630.422368`; finite progress and no fatal, persistent infeasible, NaN, or Inf signal. This is a progress observation, not the scheduled 1h health checkpoint.
- Hourly health check 1 (`2026-09-01 02:57:32 +08:00`, training elapsed `0.9407 h`): launcher and MATLAB alive/responding; iteration 64; cuts 67,264; LB `55668.3663999868`; latest iteration increment finite; anomaly scan found no fatal exception, NaN, Inf, infeasible, or out-of-memory signal. Disk free `170.23 GiB`; MATLAB working set/private commit about `14.46/34.37 GiB`; system commit headroom about `9.48 GiB`. No intermediate checkpoint exists, which is expected for the inherited fixed-5h runner: it writes and validates `checkpoint_final.mat` only after the wall-clock loop ends. Health status: `PASS`, continue without intervention.
- Downstream gate hardening during training: OOS now requires matching formal completion marker, `CURRENT_STATUS=COMPLETED`, `PHASE=CLEAN_RELOAD_PASS`, run ID, checkpoint parent run ID, checkpoint SHA, and candidate TerminalLOH SHA. Future training starts clear stale training/OOS/analysis completion markers after acquiring the training lock. These are lifecycle-only changes; the active formal MATLAB process and model science were not changed.
- Analysis preflight: Python compile, PowerShell syntax, early-gate rejection, and an in-memory unit smoke for zero-target handling, changed-target migration, quantity/location decomposition, and capped path service all passed. Formal raw data has not been analyzed yet because OOS remains gated.
- Mode C grouping parity was mechanically aligned to the accepted repository closeout implementation: target groups use Base positive-target q33/q67 thresholds frozen by path ID; tail groups use retrospective Base terminal-site-gap rank with path-ID tie breaking (`NORMAL95=9500`, `DIFFICULT4=400`, `EXTREME1=99`, `WORST_PATH=1`). The tail-rule unit smoke passed. Candidate target values are allowed to change because TerminalLOH is the authorized changed input; group membership remains frozen from Base for paired analysis.
- Downstream schema contract audit: `diagnostics/MODE_C_SCHEMA_CONTRACT_QA.json` is `PASS`. The candidate MATLAB serializer was mechanically compared with every direct and indirect Python analyzer dependency for path, stage, stage-site, hour-site, hour-system, HTT OD, and OOS metadata tables; all required columns are present and every table has `missing=0`. This is static preflight only; no formal OOS data exists yet.
- MSP-only-change audit: `manifests/ONLY_TERMINALLOH_REPLACED_QA.json` is `PASS`. Against the canonical Stage89Q Base program, 47/49 common program files are byte-identical; the only two changed files are the two TerminalLOH loaders, and both are exact authorized transforms limited to candidate table path/SHA/version/provenance gates. The candidate TerminalLOH table exactly matches the frozen SHA `70e02f...dae5`. The fixed-5h runner is an exact authorized transform of the B1011 formal 5h source, limited to the new TerminalLOH identity, Base Pmax arm `[300,200,120,150]`, isolated candidate folders, and status path. The three active runtime utils match the formal 5h source byte-for-byte. A nested duplicate utils folder is not on the MATLAB path and is recorded as inactive, not used. `BASE_MSP_IDENTITY_REUSED=YES`; `ONLY_TERMINALLOH_REPLACED=YES`; candidate remains not adopted.
- Continuous-chain handoff (`2026-09-01 03:31:42 +08:00`): candidate-local `CONTINUE_CHAIN.ps1` started as hidden PowerShell PID `37072` while formal MATLAB PID `4408` remained alive. It polls completion gates every 60 seconds without inspecting individual iterations. It can start OOS only after `MODE=FORMAL`, `CLEAN_RELOAD=PASS`, `CURRENT_STATUS=COMPLETED`, matching run ID/checkpoint path/SHA, and candidate TerminalLOH SHA all pass; it can start analysis only after `OOS_COMPLETED.txt`. `START_OOS.ps1` and `START_ANALYSIS.ps1` now use exclusive PID locks, so the watcher and hourly thread heartbeat cannot duplicate a downstream run. Parser checks and premature-start rejection checks passed before launch.
- Watcher restart-point repair (`2026-09-01 03:37:24 +08:00`): audit found that a watcher process restarted specifically between completed OOS and analysis could incorrectly depend on the training clean-reload value still being present in `CURRENT_STATUS`, even though the OOS evaluator legitimately updates that file. `CONTINUE_CHAIN.ps1` now treats a valid `OOS_COMPLETED.txt` (`MODE=C`, 10,000 paths) as an independent restart point and resumes analysis directly. Only the auxiliary watcher was restarted (`37072 -> 28648`); the formal MATLAB PID `4408` remained alive and responsive. The old watcher lock was preserved as `STALE_CHAIN_WATCHER_LOCK_20260901_033724.txt`; new watcher stderr is empty.
- Hourly health check 2 (`2026-09-01 04:01 +08:00`, nearest formal trace elapsed `7179.425 s = 1.9943 h`): MATLAB and watcher alive/responding; iteration 133; cuts 139,783; LB `56192.2326578352`; finite LB increment `0.439168630276981`; trace warning ID/message empty; no fatal exception, persistent infeasibility, NaN, or Inf signal in the active logs. Disk free `170.23 GiB`; system commit headroom about `9.14 GiB`. No final checkpoint yet, as expected before the inherited fixed-5h loop ends. Health status: `PASS`, continue without intervention.
- Hourly health check 3 (`2026-09-01 05:01 +08:00`, nearest formal trace elapsed `10802.412 s = 3.0007 h`): MATLAB and watcher alive/responding; iteration 198; cuts 208,098; LB `56307.8935933424`; finite LB increment `0.00242976108711446`; trace warning ID/message empty; no fatal or persistent-invalidity signal. Disk free about `170.20 GiB`; system commit headroom about `8.79 GiB`. The process remained responsive with stable output growth; health status: `PASS`, continue without intervention.
- Hourly health check 4 (`2026-09-01 06:02 +08:00`, nearest formal trace elapsed `14397.361 s = 3.9993 h`): MATLAB and watcher alive/responding; iteration 260; cuts 273,260; LB `56344.0252700588`; finite LB increment `0.00506520165072288`; trace warning ID/message empty; no fatal or persistent-invalidity signal. Disk free `169.97 GiB`; system commit headroom about `8.49 GiB`. No final checkpoint yet, which is expected before the fixed 18,000-second loop ends. Health status: `PASS`, continue without intervention.
- Formal 5h training finished (`2026-09-01 07:02:09 +08:00`): fixed loop completed `321` iterations in `18004.7543188 s`, final LB `56436.8825620545`, cumulative cuts `337371`, zero warnings/errors, and full-cut audit PASS. Training identity is `FIXED_5H_BUDGET_TRAINING`; convergence scope remains `NOT_CONVERGENCE_CERTIFICATE`.
- Auto-repair after training: the checkpoint had been fully written (330,553,125 bytes), but the outer PowerShell finalizer failed because `Get-FileHash` was unavailable in that launcher process. No training result was lost and no iteration was rerun. Hash gates were changed to a deterministic .NET SHA-256 helper, then `FINALIZE_EXISTING_TRAINING.ps1` ran only the inherited `RELOAD` phase against the existing checkpoint. All 14 reload identity/minimal-solve checks passed. Frozen checkpoint SHA-256: `9cac7bf2824476f532abc6ff4b80d04ca68449240bcb19729dc10d1849856353`.
- Formal Mode C OOS completed (`2026-09-01 07:54:10 +08:00`): 10,000 canonical ordered equal-weight paths, bank SHA `6bf3d1190a402ded052b8d3d08ed369042236e151f562bdd9fd4db7b8ff386c6`, 35,736 operating solves, wall time `1417.7428228 s`. Checkpoint SHA and 337,371 cuts were unchanged before/after OOS; max equality residual `1.1866e-12`, max inequality violation `6.9849e-10`; metadata PASS and full raw marker present.
- Base-vs-candidate Mode C analysis completed (`2026-09-01 07:55:18 +08:00`): analysis, communication, and data-preservation QA all PASS; `OVERALL_DELIVERY=COMPLETE`. Candidate mass-weighted capped service rate is `92.777010%` versus Base `91.576350%` (`+1.200659` percentage points). Sitewise failures are `446/1123` versus Base `473/1123`; PURE_QUANTITY `89` vs `118`, PURE_LOCATION `211` vs `203`, MIXED `146` vs `152`. Candidate remains `NOT YET FORMALLY ADOPTED`.
