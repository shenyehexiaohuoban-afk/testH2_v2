# BASE2-SAA Reconstructed Source Closeout

历史运行源码原始字节级身份未得到证明。历史 `.pyc` 与当前 reconstructed source 在规范化运行字节码层面一致；当前 reconstructed source 独立重算 35-state SAA TerminalLOH，完整重现历史冻结输出，35x4 T、总量、objective、recourse、QA 均零差异，且 reconstructed table 与历史 table SHA-256 完全一致。

因此当前身份是 `RECONSTRUCTED_BYTECODE_EQUIVALENT_SOURCE`，不是找回了历史源码。数值复现证据、PYC forensic 证据和输入 provenance 均保存在本目录及 `reconstruction-20260905-run-004` 中。
